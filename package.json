{
  "name": "vitrine_backend",
  "version": "0.1.0",
  "description": "Backend Rust/Axum pour CUBI",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "seed": "cargo run --bin seed",
    "migrate": "sqlx migrate run",
    "migrate:revert": "sqlx migrate revert",
    "check": "cargo check",
    "fmt": "cargo fmt",
    "clippy": "cargo clippy"
  },
  "private": true
}
