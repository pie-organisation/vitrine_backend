services:
  - type: web
    name: vitrine-backend
    runtime: node
    buildCommand: yarn install --frozen-lockfile
    startCommand: yarn start
    envVars:
      - key: NODE_ENV
        value: production
